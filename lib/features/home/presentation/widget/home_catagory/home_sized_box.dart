import 'package:flutter/cupertino.dart';

class HomeSizedBox extends StatelessWidget {
  const HomeSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15,);
  }
}
